<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Singapore Bank Analytics — Portfolio Dashboard</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,600&family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.5.0/chart.umd.min.js"></script>
<style>
  :root{
    --ink:#14222F;
    --ink-soft:#4B5A68;
    --ink-faint:#7C8A96;
    --paper:#EEF1F0;
    --card:#FFFFFF;
    --rule:rgba(20,34,47,0.14);
    --rule-strong:rgba(20,34,47,0.28);
    --jade:#0F7A5C;
    --jade-d:#0B5943;
    --amber:#C2790F;
    --amber-d:#8C5708;
    --red:#A93226;
    --red-d:#7A241B;
  }
  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    background:var(--paper);
    color:var(--ink);
    font-family:'IBM Plex Sans',sans-serif;
    -webkit-font-smoothing:antialiased;
  }
  .wrap{max-width:880px;margin:0 auto;padding:0 28px;}
  .top-rule{height:3px;background:var(--ink);}

  /* ---------- Hero / masthead ---------- */
  header.masthead{padding:56px 0 32px;border-bottom:1px solid var(--rule-strong);}
  .eyebrow{
    font-family:'IBM Plex Mono',monospace;
    font-size:11px;
    letter-spacing:0.14em;
    text-transform:uppercase;
    color:var(--ink-soft);
    margin:0 0 14px;
  }
  h1{
    font-family:'Source Serif 4',serif;
    font-weight:600;
    font-size:clamp(32px,5vw,46px);
    line-height:1.08;
    margin:0 0 14px;
    letter-spacing:-0.01em;
  }
  .lede{
    font-size:16px;
    line-height:1.6;
    color:var(--ink-soft);
    max-width:560px;
    margin:0 0 24px;
  }
  .hero-row{display:flex;align-items:center;gap:16px;flex-wrap:wrap;}
  .disclaimer-chip{
    display:inline-flex;
    align-items:center;
    gap:8px;
    font-family:'IBM Plex Mono',monospace;
    font-size:11.5px;
    color:var(--amber-d);
    background:rgba(194,121,15,0.1);
    border:1px solid rgba(194,121,15,0.3);
    padding:7px 12px;
    border-radius:2px;
  }
  .disclaimer-chip .dot{width:6px;height:6px;border-radius:50%;background:var(--amber);flex:none;}
  .cta{
    font-family:'IBM Plex Mono',monospace;
    font-size:12.5px;
    color:var(--ink);
    text-decoration:none;
    border-bottom:1px solid var(--ink);
    padding-bottom:2px;
    transition:opacity .15s ease;
  }
  .cta:hover{opacity:0.6;}

  /* ---------- Ledger / KPI strip ---------- */
  section.ledger{padding:36px 0;border-bottom:1px solid var(--rule-strong);}
  .section-label{
    font-family:'IBM Plex Mono',monospace;
    font-size:11px;
    letter-spacing:0.12em;
    text-transform:uppercase;
    color:var(--ink-faint);
    margin:0 0 18px;
  }
  .ledger-row{
    display:flex;
    align-items:baseline;
    gap:12px;
    padding:13px 0;
    border-bottom:1px dotted var(--rule);
    transition:background .15s ease;
  }
  .ledger-row:hover{background:rgba(15,122,92,0.05);}
  .ledger-row:last-child{border-bottom:none;}
  .ledger-label{
    font-size:14px;
    color:var(--ink-soft);
    white-space:nowrap;
  }
  .ledger-leader{
    flex:1;
    border-bottom:1px dotted var(--rule);
    transform:translateY(-4px);
  }
  .ledger-value{
    font-family:'IBM Plex Mono',monospace;
    font-size:18px;
    font-weight:500;
    white-space:nowrap;
  }
  .ledger-value.warn{color:var(--amber-d);}
  .ledger-value.crit{color:var(--red-d);}
  .ledger-value.ok{color:var(--jade-d);}

  /* ---------- Charts ---------- */
  section.charts{padding:40px 0;border-bottom:1px solid var(--rule-strong);}
  .chart-grid{
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:20px;
  }
  .chart-card{
    background:var(--card);
    border:1px solid var(--rule);
    border-radius:4px;
    padding:20px 20px 14px;
  }
  .chart-title{
    font-size:13.5px;
    font-weight:500;
    margin:0 0 2px;
  }
  .chart-sub{
    font-family:'IBM Plex Mono',monospace;
    font-size:11px;
    color:var(--ink-faint);
    margin:0 0 14px;
  }
  .chart-card canvas{max-height:200px;}

  /* ---------- Project index ---------- */
  section.index{padding:40px 0;border-bottom:1px solid var(--rule-strong);}
  .index-row{
    display:flex;
    gap:20px;
    padding:18px 0;
    border-bottom:1px solid var(--rule);
    text-decoration:none;
    color:inherit;
    position:relative;
  }
  .index-row:last-child{border-bottom:none;}
  .index-row::before{
    content:'';
    position:absolute;
    left:-28px;top:0;bottom:0;
    width:3px;
    background:var(--ink);
    opacity:0;
    transition:opacity .15s ease;
  }
  .index-row:hover::before{opacity:1;}
  .index-row:hover .index-title{color:var(--jade-d);}
  .index-num{
    font-family:'IBM Plex Mono',monospace;
    font-size:13px;
    color:var(--ink-faint);
    flex:none;
    width:28px;
    padding-top:2px;
  }
  .index-title{
    font-size:15.5px;
    font-weight:500;
    margin:0 0 4px;
    transition:color .15s ease;
  }
  .index-files{
    font-size:13px;
    color:var(--ink-soft);
    line-height:1.5;
    margin:0;
  }
  .index-arrow{
    font-family:'IBM Plex Mono',monospace;
    font-size:13px;
    color:var(--ink-faint);
    flex:none;
    align-self:center;
  }

  /* ---------- Footer / contact ---------- */
  footer{padding:40px 0 56px;}
  .sig-label{
    font-family:'IBM Plex Mono',monospace;
    font-size:11px;
    letter-spacing:0.12em;
    text-transform:uppercase;
    color:var(--ink-faint);
    margin:0 0 16px;
  }
  .sig-name{
    font-family:'Source Serif 4',serif;
    font-size:22px;
    margin:0 0 18px;
  }
  .contact-list{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:9px;}
  .contact-list li{
    display:flex;
    gap:14px;
    font-size:14px;
    align-items:baseline;
  }
  .contact-list .k{
    font-family:'IBM Plex Mono',monospace;
    font-size:11px;
    letter-spacing:0.08em;
    text-transform:uppercase;
    color:var(--ink-faint);
    width:74px;
    flex:none;
  }
  .contact-list a{color:var(--ink);text-decoration:none;border-bottom:1px solid var(--rule-strong);}
  .contact-list a:hover{border-bottom-color:var(--ink);}
  .footnote{
    margin-top:28px;
    font-size:12px;
    color:var(--ink-faint);
    line-height:1.6;
  }

  .reveal{opacity:0;transform:translateY(10px);transition:opacity .5s ease,transform .5s ease;}
  .reveal.in{opacity:1;transform:none;}
  @media (prefers-reduced-motion:reduce){.reveal{opacity:1;transform:none;transition:none;}}

  @media (max-width:680px){
    .chart-grid{grid-template-columns:1fr;}
    .ledger-label{white-space:normal;}
  }
</style>
</head>
<body>

<div class="top-rule"></div>
<div class="wrap">

  <header class="masthead reveal">
    <p class="eyebrow">SQL portfolio — retail &amp; consumer banking, Singapore</p>
    <h1>Singapore Bank Analytics</h1>
    <p class="lede">A PostgreSQL portfolio modeling customer analytics, credit risk, net interest margin, and MAS regulatory reporting for a Singapore retail bank.</p>
    <div class="hero-row">
      <span class="disclaimer-chip"><span class="dot"></span>Illustrative metrics — computed from the seed dataset, not live banking data</span>
      <a class="cta" href="https://github.com/prithikashreeravi-lab/singapore-banking-sql-analytics" target="_blank" rel="noopener">View source on GitHub →</a>
    </div>
  </header>

  <section class="ledger reveal">
    <p class="section-label">Portfolio summary — seed dataset</p>
    <div class="ledger-row">
      <span class="ledger-label">Customers on book</span>
      <span class="ledger-leader"></span>
      <span class="ledger-value">20</span>
    </div>
    <div class="ledger-row">
      <span class="ledger-label">Active loan accounts</span>
      <span class="ledger-leader"></span>
      <span class="ledger-value">10</span>
    </div>
    <div class="ledger-row">
      <span class="ledger-label">Total loan book outstanding</span>
      <span class="ledger-leader"></span>
      <span class="ledger-value">SGD 3,996,200</span>
    </div>
    <div class="ledger-row">
      <span class="ledger-label">NPL ratio</span>
      <span class="ledger-leader"></span>
      <span class="ledger-value ok">0.25%</span>
    </div>
    <div class="ledger-row">
      <span class="ledger-label">Loans in delinquency (DPD &gt; 0, pre-NPL)</span>
      <span class="ledger-leader"></span>
      <span class="ledger-value warn">2</span>
    </div>
    <div class="ledger-row">
      <span class="ledger-label">Deposit &amp; loan products modeled</span>
      <span class="ledger-leader"></span>
      <span class="ledger-value">11</span>
    </div>
  </section>

  <section class="charts reveal">
    <p class="section-label">Seed data — visualized</p>
    <div class="chart-grid">
      <div class="chart-card">
        <p class="chart-title">Customers by segment</p>
        <p class="chart-sub">MASS / EMERGING / AFFLUENT / PRIORITY / PRIVATE</p>
        <canvas id="chartSegments"></canvas>
      </div>
      <div class="chart-card">
        <p class="chart-title">Loan book by product type</p>
        <p class="chart-sub">Outstanding, SGD</p>
        <canvas id="chartProducts"></canvas>
      </div>
      <div class="chart-card">
        <p class="chart-title">DPD bucket distribution</p>
        <p class="chart-sub">Loan count by days-past-due bucket</p>
        <canvas id="chartDPD"></canvas>
      </div>
      <div class="chart-card">
        <p class="chart-title">Loan status</p>
        <p class="chart-sub">Current / delinquent / NPL</p>
        <canvas id="chartStatus"></canvas>
      </div>
    </div>
  </section>

  <section class="index reveal">
    <p class="section-label">Analysis index</p>

    <a class="index-row" href="https://github.com/prithikashreeravi-lab/singapore-banking-sql-analytics/tree/main/analysis/01_customer_analytics" target="_blank" rel="noopener">
      <span class="index-num">01</span>
      <div style="flex:1">
        <p class="index-title">Customer analytics</p>
        <p class="index-files">rfm_segmentation.sql — RFM scoring with churn flags<br>cohort_retention.sql — monthly acquisition cohort retention</p>
      </div>
      <span class="index-arrow">→</span>
    </a>

    <a class="index-row" href="https://github.com/prithikashreeravi-lab/singapore-banking-sql-analytics/tree/main/analysis/02_loan_risk" target="_blank" rel="noopener">
      <span class="index-num">02</span>
      <div style="flex:1">
        <p class="index-title">Loan risk</p>
        <p class="index-files">delinquency_aging.sql — DPD buckets, roll rate matrix<br>vintage_analysis.sql — loss curves by origination cohort<br>mortgage_ltv.sql — LTV distribution, MAS TDSR stress test</p>
      </div>
      <span class="index-arrow">→</span>
    </a>

    <a class="index-row" href="https://github.com/prithikashreeravi-lab/singapore-banking-sql-analytics/tree/main/analysis/03_revenue_nim" target="_blank" rel="noopener">
      <span class="index-num">03</span>
      <div style="flex:1">
        <p class="index-title">Revenue &amp; NIM</p>
        <p class="index-files">net_interest_income.sql — NII waterfall<br>product_profitability.sql — revenue by product, NIM by segment</p>
      </div>
      <span class="index-arrow">→</span>
    </a>

    <a class="index-row" href="https://github.com/prithikashreeravi-lab/singapore-banking-sql-analytics/tree/main/analysis/04_regulatory" target="_blank" rel="noopener">
      <span class="index-num">04</span>
      <div style="flex:1">
        <p class="index-title">Regulatory</p>
        <p class="index-files">mas610_loans.sql — MAS 610 Table 1A/1B loan returns<br>npl_schedule.sql — NPA schedule, MAS Notice 612</p>
      </div>
      <span class="index-arrow">→</span>
    </a>
  </section>

  <footer class="reveal">
    <p class="sig-label">Prepared by</p>
    <p class="sig-name">[Your name]</p>
    <ul class="contact-list">
      <li><span class="k">Email</span><a href="mailto:you@example.com">you@example.com</a></li>
      <li><span class="k">LinkedIn</span><a href="https://linkedin.com/in/your-handle" target="_blank" rel="noopener">linkedin.com/in/your-handle</a></li>
      <li><span class="k">GitHub</span><a href="https://github.com/prithikashreeravi-lab" target="_blank" rel="noopener">github.com/prithikashreeravi-lab</a></li>
    </ul>
    <p class="footnote">All figures on this page are computed from the illustrative seed data in seed/sample_data.sql for demonstration purposes and do not represent any real institution's data.</p>
  </footer>

</div>

<script>
  const ink = '#14222F';
  const inkSoft = '#4B5A68';
  const rule = 'rgba(20,34,47,0.10)';

  Chart.defaults.font.family = "'IBM Plex Mono', monospace";
  Chart.defaults.font.size = 11;
  Chart.defaults.color = inkSoft;

  const baseGrid = { color: rule, drawTicks: false };
  const baseOpts = {
    responsive: true,
    maintainAspectRatio: false,
    plugins: { legend: { display: false } },
  };

  new Chart(document.getElementById('chartSegments'), {
    type: 'bar',
    data: {
      labels: ['Private', 'Priority', 'Affluent', 'Emerging', 'Mass'],
      datasets: [{
        data: [2, 2, 4, 4, 8],
        backgroundColor: ['#0B3D33', '#11614F', '#1D8A6E', '#5FB89A', '#A9D9C7'],
        borderRadius: 2,
        barThickness: 18,
      }]
    },
    options: {
      ...baseOpts,
      indexAxis: 'y',
      scales: {
        x: { grid: baseGrid, ticks: { stepSize: 2 } },
        y: { grid: { display: false } }
      }
    }
  });

  new Chart(document.getElementById('chartProducts'), {
    type: 'bar',
    data: {
      labels: ['Mortgage', 'Auto', 'Renovation', 'Personal'],
      datasets: [{
        data: [3880000, 58000, 31000, 27200],
        backgroundColor: '#1F3A55',
        borderRadius: 2,
        barThickness: 18,
      }]
    },
    options: {
      ...baseOpts,
      scales: {
        x: { grid: { display: false } },
        y: { grid: baseGrid, ticks: { callback: v => (v/1000) + 'k' } }
      }
    }
  });

  new Chart(document.getElementById('chartDPD'), {
    type: 'bar',
    data: {
      labels: ['Current', '1-30', '31-60', '61-90', '>90'],
      datasets: [{
        data: [7, 1, 0, 1, 1],
        backgroundColor: ['#0F7A5C', '#D9A441', '#C2790F', '#A85A1E', '#A93226'],
        borderRadius: 2,
        barThickness: 18,
      }]
    },
    options: {
      ...baseOpts,
      scales: {
        x: { grid: { display: false } },
        y: { grid: baseGrid, ticks: { stepSize: 2 } }
      }
    }
  });

  new Chart(document.getElementById('chartStatus'), {
    type: 'doughnut',
    data: {
      labels: ['Current', 'Delinquent', 'NPL'],
      datasets: [{
        data: [7, 2, 1],
        backgroundColor: ['#0F7A5C', '#C2790F', '#A93226'],
        borderWidth: 0,
      }]
    },
    options: {
      ...baseOpts,
      cutout: '64%',
      plugins: {
        legend: {
          display: true,
          position: 'bottom',
          labels: { boxWidth: 10, boxHeight: 10, padding: 14, font: { size: 11 } }
        }
      }
    }
  });

  if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const io = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('in'); });
    }, { threshold: 0.1 });
    document.querySelectorAll('.reveal').forEach(el => io.observe(el));
  } else {
    document.querySelectorAll('.reveal').forEach(el => el.classList.add('in'));
  }
</script>

</body>
</html>
